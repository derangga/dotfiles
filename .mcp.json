{
  "mcpServers": {
    "serena": {
      "command": "sh",
      "args": [
        "-c",
        "uvx --from git+https://github.com/oraios/serena serena start-mcp-server --project $(pwd)"
      ]
    }
  }
}
